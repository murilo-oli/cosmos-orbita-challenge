namespace Cosmos.Application.Enums;

public enum EResponseType
{
    NOT_FOUND,
    INVALID_CREDENTIALS,
    FORBIDDEN,
    INVALID_MODEL,
    ALREADY_EXIST,
    SUCCESSFUL,
    CREATED_SUCCESSFUL,
    BAD_REQUEST
}
